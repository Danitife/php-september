let courses = ["Php", "Html", "Css"]

console.log(courses[1]);

for (let index = 0; index < courses.length; index++) {
    const element = courses[index];
    console.log(`My best course is ${element}`);
}